# Prone2Stroke

A collection of AI-tools to aid medical professionals in their work.


## Project setup (Windows)

Create a virtual environment for your python requirements.

`python -m venv venv`

Activate the virtual environment:

`venv/Scripts/activate.bat`

Install python requirements:
`python -m pip install -r requirements.txt`

Go to the frontend folder and install npm modules:
`cd frontend`
`npm install`

### Run the backend
`py main.py`

### Run the frontend
`npm run dev`